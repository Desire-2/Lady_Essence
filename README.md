# Lady’s Essence USSD System

This is a USSD-based system for managing maternal and child health, nutrition, and related services.

## Installation
1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`.
3. Run the application: `python app.py`.

## Testing
Run unit tests: `python -m unittest tests/test_services.py`.

## License
MIT